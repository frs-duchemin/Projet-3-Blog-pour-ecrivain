    <div class="table-responsive">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th class="text-center"; width="50%">{{ 'Administrateur'|trans }}</th>
                    <th scope="col" class="text-center"; width="50%"><i class="fa fa-cogs" aria-hidden="true"></i> {{ 'Action'|trans }}</th>
                </tr>
            </thead>

            {% for user in users %}
                <tr>
                    <td class="text-center">{{ user.username }}</a></td>
                    <td class="text-center">
                        <a href="{{ path('admin_user_edit', { 'id': user.id }) }}" class="btn btn-sm btn-primary">
                            <i class="fa fa-edit" aria-hidden="true"></i> {{ 'Editer'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
</div>